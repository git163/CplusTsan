// src/main.cpp — entry point
// Placeholder. Delete or replace once real modules are added.

#include <iostream>

int main(int /*argc*/, char* /*argv*/[]) {
    std::cout << "CplusTsan started" << std::endl;
    return 0;
}