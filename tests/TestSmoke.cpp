// tests/TestSmoke.cpp — sanity check that GTest is wired up correctly.
// Delete this file once you have your first real test.

#include <gtest/gtest.h>

TEST(SmokeTest, TrivialAssertion) {
    EXPECT_EQ(1 + 1, 2);
}